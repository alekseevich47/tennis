# Локальные переменные Vite / MAX (не коммитить client/.env).
# Скопируйте в client/.env и заполните значения.

VITE_POCKETBASE_URL=
VITE_API_URL=
VITE_MAX_AUTH_URL=
VITE_MAX_APP_ID=

# Секрет бота MAX — только на сервере / в локальном .env, никогда в git.
MAX_BOT_TOKEN=
